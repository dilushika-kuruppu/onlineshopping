using System;
using System.Web;
using System.Web.UI.WebControls;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Microsoft.Win32;
using Moq;

namespace OnlineShopping.UnitTests
{
    [TestClass]
    public class LoginTests
    {
        [TestMethod]
        public void Login_UserIdentify_ReturnTure()
        {

            //Arrange
 
//var login = new Login();
        // string UserName = "dilu";
        //  string Password = "dilu";

            //Act
          //var   flag = login.Login(UserName, Password);

            //Assert
          //Assert.IsTrue(flag, "Login in failed for user: [" + UserName + "]");
        }
    }
}
